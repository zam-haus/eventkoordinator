<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .proposal-title {
            font-weight: bold;
        }
        .cta-link {
            display: inline-block;
            margin: 8px 0 20px;
            color: #0066cc;
            word-break: break-all;
        }
        .divider {
            border: none;
            border-top: 2px solid #ddd;
            margin: 30px 0;
        }
        .lang-label {
            font-size: 0.75em;
            color: #999;
            text-transform: uppercase;
            letter-spacing: 0.1em;
        }
    </style>
</head>
<body>
    <p class="lang-label">Deutsch</p>

    <p>Der folgende Termin für <span class="proposal-title">"{{ proposal.call_title }}"</span> wurde abgesagt:</p>

    <p><strong>Veranstaltung:</strong> <span class="proposal-title">{{ event.name }}</span><br>
    <strong>Einreichung:</strong> {{ proposal.title }}<br>
    <strong>Einreicher:</strong> {{ proposal.owner_name }}<br>
    <strong>Beginn:</strong> {{ event.start_time | timezone("Europe/Berlin") | isoformat() }}<br>
    <strong>Ende:</strong> {{ event.end_time | timezone("Europe/Berlin") | isoformat() }}</p>

    <p>Einreichung ansehen:</p>
    <a class="cta-link" href="{{ proposal.url }}">{{ proposal.url }}</a>

    <p>Termin ansehen:</p>
    <a class="cta-link" href="{{ event.url }}">{{ event.url }}</a>

    <hr class="divider">

    <p class="lang-label">English</p>

    <p>The following date for <span class="proposal-title">"{{ proposal.call_title }}"</span> has been canceled:</p>

    <p><strong>Event:</strong> <span class="proposal-title">{{ event.name }}</span><br>
    <strong>Submission:</strong> {{ proposal.title }}<br>
    <strong>Submitted by:</strong> {{ proposal.owner_name }}<br>
    <strong>Start:</strong> {{ event.start_time | timezone("Europe/Berlin") | isoformat() }}<br>
    <strong>End:</strong> {{ event.end_time | timezone("Europe/Berlin") | isoformat() }}</p>

    <p>View submission:</p>
    <a class="cta-link" href="{{ proposal.url }}">{{ proposal.url }}</a>

    <p>View date:</p>
    <a class="cta-link" href="{{ event.url }}">{{ event.url }}</a>
</body>
</html>
