[Deutsch]

Hi!

Wir möchten dir mitteilen, dass ein neuer Terminvorschlag für deine Einreichung "{{ proposal.title }}" für "{{ proposal.call_title }}" erstellt wurde:

  Veranstaltung: {{ event.name }}
  Beginn: {{ event.start_time | timezone("Europe/Berlin") | isoformat() }}
  Ende: {{ event.end_time | timezone("Europe/Berlin") | isoformat() }}

Du kannst den Terminvorschlag unter folgendem Link ansehen und bestätigen oder ablehnen:

{{ event.url }}

Bei Fragen melde dich gerne bei uns.

Das {{ proposal.call_title }} Team

================================

[English]

Hi!

We would like to let you know that a new date proposal has been created for your submission "{{ proposal.title }}" for "{{ proposal.call_title }}":

  Event: {{ event.name }}
  Start: {{ event.start_time | timezone("Europe/Berlin") | isoformat() }}
  End: {{ event.end_time | timezone("Europe/Berlin") | isoformat() }}

You can view and confirm or reject the date proposal at the following link:

{{ event.url }}

Please don't hesitate to reach out if you have any questions.

The {{ proposal.call_title }} Team
