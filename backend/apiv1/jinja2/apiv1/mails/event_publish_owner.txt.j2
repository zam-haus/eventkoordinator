{% set call_title = object.proposal.call.title if object.proposal and object.proposal.call else '' %}
{% set proposal_title = object.proposal.title if object.proposal else object.name %}
[Deutsch]

Hi!

Der folgende Termin für deine Einreichung "{{ proposal_title }}" für "{{ call_title }}" wird nun veröffentlicht:

  Veranstaltung: {{ object.name }}
  Beginn: {{ object.start_time.strftime('%d.%m.%Y %H:%M') }}
  Ende: {{ object.end_time.strftime('%d.%m.%Y %H:%M') }}

Den Termin kannst du unter folgendem Link einsehen:

{{ event_url }}

Bei Fragen melde dich gerne bei uns.

Das {{ call_title }} Team

================================

[English]

Hi!

The following date for your submission "{{ proposal_title }}" for "{{ call_title }}" is now being published:

  Event: {{ object.name }}
  Start: {{ object.start_time.strftime('%Y-%m-%d %H:%M') }}
  End: {{ object.end_time.strftime('%Y-%m-%d %H:%M') }}

You can view the date at the following link:

{{ event_url }}

Please don't hesitate to reach out if you have any questions.

The {{ call_title }} Team
